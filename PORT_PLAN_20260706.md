# Plan de portage — travail du 2-6 juillet fait par erreur dans ~/Documents/00-code/0rum

> Contexte : entre le 2 et le 6 juillet 2026, des sessions assistant ont travaillé dans
> `~/Documents/00-code/0rum` (moteur de recherche, Docker :8008) alors que la cible était
> CE repo (bot live 127.0.0.1:8787). Cause : typo `Application/0rum` le 2 juillet (~09:40 Paris),
> documentée dans mem0. Ce document liste ce qui doit être porté ici, dans l'ordre.
> Le snapshot de sécurité pré-portage est le commit `9877dba`.

## Source du travail à porter

Repo : `~/Documents/00-code/0rum` — 26 fichiers modifiés (+1 357/-232) + fichiers non suivis.
Les deux codebases sont DIFFÉRENTES (src/ moteur walk-forward vs orum/ bot DSL/ak_macd) :
c'est un portage de concepts, pas un copier-coller de fichiers.

## Bucket A — À PORTER ICI (fonctionnalités bot live)

1. **Multi-instrument / ETH sur le dashboard** (la demande d'origine de l'utilisateur)
   - Source : `src/market/instruments.py` (+13), `src/monitoring/dashboard.py` (+96),
     `src/templates/dashboard.html` (+162)
   - Cible : `orum/dashboard.py`, `orum/static/dashboard.{html,js,css}`, `orum/adapters/price.py`
   - Note : des caches ETHUSDT/PAXGUSDT klines existent déjà dans `state/data_cache/`
2. **Tradeability screener (Step 0)** — bloque le trading si feed H1 mort (>3h)
   - Source : `src/pipeline/tradeability.py` + `tests/test_pipeline/test_tradeability.py`
   - Cible : à insérer en amont de la décision dans `orum/loop.py` / `orum/external/orchestrator.py`
   - Priorité haute : c'est une protection de bot LIVE par excellence
3. **Stress-cost gate** — veto si le trade ne survit pas à des coûts stressés (×N spread/slippage/fees)
   - Source : `src/execution/cost_models.py` (+30) + `tests/test_execution/test_cost_models.py`
   - Cible : `orum/executor.py` ou `orum/score.py` (avant exécution)

## Bucket B — RESTE dans ~/Documents/00-code/0rum (travail de recherche, au bon endroit)

- Optimizer IS-only fix, walk_forward (+47), regime_detector, bayesian.py,
  fixes stratégies (trend_continuation/ema_momentum/breakout_expansion),
  backfill_history.py (760 jours), runs optimizer du 02/07 (`var/logs/`)
- Ces éléments améliorent le LABO. Le lien avec le bot live = les paramètres de stratégies
  validés par walk-forward, pas le code.
- ⚠️ Ce repo a aussi 26 M + fichiers non suivis NON COMMITÉS → faire un commit de sauvegarde là-bas aussi.

## Bucket C — Documentation/données à copier si souhaité

- `research/*.md` du 02/07 (edge-research-france, btc-sessions, mica-eur-premium, event-vol,
  cac-flux, ucits-fv-gap) → copiables dans `docs/research/` ici
- Ticks ETHUSD Dukascopy → restent côté recherche (données de labo)

## Contraintes d'exécution

- Les process live (worker/watcher/dashboard/producer) ne sont JAMAIS redémarrés sans
  confirmation explicite de l'utilisateur. Les changements de code ne prennent effet
  qu'au prochain redémarrage → implémenter sur la branche, puis demander une fenêtre de restart.
- Après chaque élément porté : lancer les tests du repo (`tests/`), commit atomique.

## État

- [x] Snapshot de sécurité bot live (`9877dba`, 2026-07-06)
- [x] Bannières de routage dans les 3 CLAUDE.md/AGENTS.md
- [ ] Commit de sauvegarde côté Documents
- [ ] A1 multi-instrument/ETH
- [ ] A2 tradeability screener
- [ ] A3 stress-cost gate
- [ ] C docs research (optionnel)
- [ ] Fenêtre de redémarrage validée par l'utilisateur → déploiement
