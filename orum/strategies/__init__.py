"""Strategy engine plugins.

Package marker only at this commit. The registry that loads an engine by
name from ``goal.yaml`` lands in a later commit once at least two engines
(``native_dsl``, ``ak_macd``) exist to register.
"""
