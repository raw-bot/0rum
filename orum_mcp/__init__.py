"""orum-mcp — read-only MCP (Model Context Protocol) server over 0rum state.

Isolated add-on module: nothing under orum/ imports this package, and deleting
orum_mcp/ leaves the bot byte-for-byte unaffected. See MCP_AUDIT.md and
docs/MCP.md.
"""

__version__ = "0.1.0"
