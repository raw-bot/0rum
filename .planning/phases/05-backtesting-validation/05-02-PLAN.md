---
phase: 05-backtesting-validation
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/backtesting/optimizer.py
  - tests/test_backtesting/test_optimizer.py
autonomous: true
requirements:
  - OPTIM-01
  - OPTIM-02

must_haves:
  truths:
    - "WalkForwardOptimizer generates exactly 100 LHS parameter combinations per strategy per run"
    - "LHS-sampled values are clamped to PARAM_RANGES bounds (max(lo, min(hi, val))) before use"
    - "Strategies with WFE < 0.50 do not write a new is_active=True row — previous active row is preserved"
    - "The data guard (_check_sufficient_data) prevents any DB write when candle counts are below minimums"
    - "The sliding 500-candle backtester window calls strategy.generate_signals() with a dict[str, list] — oldest-first order maintained"
    - "WalkForwardOptimizer.run() is a top-level async method callable from an APScheduler job with await"
  artifacts:
    - path: "src/backtesting/optimizer.py"
      provides: "WalkForwardOptimizer class with _sample_param_combinations(), _run_strategy_backtest(), _evaluate_all_combos(), _activate_best_params(), run()"
      exports: ["WalkForwardOptimizer"]
    - path: "tests/test_backtesting/test_optimizer.py"
      provides: "LHS combo count, value range, data guard, retain-previous tests"
  key_links:
    - from: "src/backtesting/optimizer.py"
      to: "src/backtesting/walk_forward.py"
      via: "from src.backtesting.walk_forward import build_windows, _compute_profit_factor, _compute_wfe, multi_window_gate_passes, _check_sufficient_data, simulate_trade_outcome, MIN_CANDLES_FOR_OPTIMIZER, TradeOutcome"
      pattern: "from src.backtesting.walk_forward import"
    - from: "src/backtesting/optimizer.py"
      to: "src/backtesting/monte_carlo.py"
      via: "from src.backtesting.monte_carlo import run_monte_carlo, monte_carlo_passes"
      pattern: "from src.backtesting.monte_carlo import"
    - from: "src/backtesting/optimizer.py"
      to: "src/models/optimizer_result.py"
      via: "AsyncSessionLocal write — OptimizerResultORM insert and is_active update"
      pattern: "OptimizerResultORM"
    - from: "src/scheduler/jobs.py"
      to: "src/backtesting/optimizer.py"
      via: "from src.backtesting.optimizer import WalkForwardOptimizer (deferred import inside job)"
      pattern: "WalkForwardOptimizer"
---

<objective>
Build WalkForwardOptimizer — the async orchestrator that ties together LHS sampling, the sliding-window backtester, WFE gating, Monte Carlo validation, and DB writes. This is the heaviest module in Phase 5.

Purpose: Complete the optimizer core so the scheduler job (Plan 03) has something to call. After this plan, `await WalkForwardOptimizer().run()` is a valid call that either activates best params or skips safely when data is insufficient.

Output: src/backtesting/optimizer.py (class WalkForwardOptimizer) and tests/test_backtesting/test_optimizer.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-backtesting-validation/05-CONTEXT.md
@.planning/phases/05-backtesting-validation/05-RESEARCH.md
@.planning/phases/05-backtesting-validation/05-01-SUMMARY.md

<interfaces>
<!-- From src/backtesting/walk_forward.py (Plan 01 output) -->
```python
from src.backtesting.walk_forward import (
    WalkForwardWindow,
    build_windows,
    _compute_profit_factor,
    _compute_wfe,
    multi_window_gate_passes,
    _check_sufficient_data,
    simulate_trade_outcome,
    TradeOutcome,
    MIN_CANDLES_FOR_OPTIMIZER,
    TRADING_DAYS_PER_MONTH,
)
```

<!-- From src/backtesting/monte_carlo.py (Plan 01 output) -->
```python
from src.backtesting.monte_carlo import run_monte_carlo, monte_carlo_passes
```

<!-- From src/models/optimizer_result.py — columns for DB write -->
```python
OptimizerResultORM(
    strategy=strategy_name,          # str
    params=best_combo,               # dict — JSONB
    train_start=window.train_start,  # datetime
    train_end=window.train_end,      # datetime
    test_start=window.oos_start,     # datetime
    test_end=window.oos_end,         # datetime
    in_sample_score=is_score,        # float → Numeric(8,5)
    oos_score=oos_score,             # float → Numeric(8,5)
    wfe=wfe,                         # float → Numeric(6,4)
    profit_factor=pf,                # float → Numeric(8,4)
    max_drawdown=max_dd,             # float → Numeric(8,5)
    win_rate=win_rate,               # float → Numeric(6,4)
    trade_count=trade_count,         # int
    is_active=True,                  # bool
)
```

<!-- From src/database.py — async session pattern -->
```python
from src.database import AsyncSessionLocal
# async with AsyncSessionLocal() as session:
#     await session.execute(...)
#     session.add(...)
#     await session.commit()
```

<!-- From src/models/candle.py — Candle ORM for DB fetch -->
```python
from src.models.candle import Candle
# Candle.instrument, Candle.timeframe, Candle.timestamp, Candle.complete
# .close, .high, .low, .open, .volume — all Decimal → cast to float()
```

<!-- Strategy STRATEGY_NAME values (from src/strategies/*) -->
<!-- "liquidity_sweep", "trend_continuation", "breakout_expansion", "ema_momentum" -->

<!-- Strategy PARAM_RANGES — each strategy has exactly 3 params -->
<!-- LiquiditySweepStrategy: {"sweep_atr_mult": (0.2, 0.8), "sl_atr_mult": (0.3, 1.0), "tp_risk_mult": (1.2, 3.0)} -->
<!-- TrendContinuationStrategy: {"pullback_ema": (10.0, 30.0), "sl_atr_mult": (0.3, 1.0), "tp_risk_mult": (1.2, 3.0)} -->
<!-- BreakoutExpansionStrategy: {"squeeze_lookback": (10.0, 30.0), "volume_mult": (1.2, 2.5), "sl_atr_mult": (0.5, 1.5)} -->
<!-- EmaMomentumStrategy: {"fast_ema": (5.0, 15.0), "slow_ema": (18.0, 30.0), "sl_atr_mult": (0.3, 1.0)} -->
<!-- PARAM_RANGES is a class-level attribute: LiquiditySweepStrategy.PARAM_RANGES -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WalkForwardOptimizer — LHS sampling, backtester, DB write</name>
  <read_first>
    - src/backtesting/walk_forward.py (confirm all imports available)
    - src/backtesting/monte_carlo.py (confirm imports)
    - src/models/optimizer_result.py (confirm all column names)
    - src/database.py (confirm AsyncSessionLocal usage pattern)
    - src/strategies/runner.py (_fetch_candles pattern to replicate in optimizer)
    - src/scheduler/jobs.py (see how existing jobs use deferred imports inside async functions)
    - .planning/phases/05-backtesting-validation/05-RESEARCH.md (Patterns 1, 7, 9 — exact function signatures and anti-patterns section)
  </read_first>

  <behavior>
    - Test: _sample_param_combinations(param_ranges, n_combos=100) returns list of exactly 100 dicts
    - Test: each combo dict has keys matching param_ranges.keys()
    - Test: all values in each combo are within [lo, hi] bounds after clamping (max(lo, min(hi, v)))
    - Test: values are floats, not numpy scalars (use isinstance(v, float))
    - Test: data guard — when _check_sufficient_data returns False, run() returns early without calling session.add() or session.commit() (mock the session and assert not called)
    - Test: retain-previous behavior — when WFE < 0.50 for all combos, the existing is_active=True row is NOT deactivated (mock DB — assert update() with values(is_active=False) is NOT called)
    - Test: when best combo passes WFE and multi-window gate, _activate_best_params is called exactly once per strategy
  </behavior>

  <action>
Create src/backtesting/optimizer.py with this exact structure and logic:

```python
"""Walk-forward optimizer — LHS sampling, backtesting, WFE gating, DB writes.

Architecture (per RESEARCH.md Sync/Async Decision):
  async entry (run()) → sync heavy compute (LHS, trade simulation) → async DB write.
  No asyncio.run() anywhere — the APScheduler job is already a coroutine.
"""

from __future__ import annotations

import asyncio
from collections import defaultdict
from datetime import datetime, timezone
from typing import Any

import numpy as np
import structlog
from scipy.stats.qmc import LatinHypercube, scale
from sqlalchemy import select, update

from src.backtesting.monte_carlo import monte_carlo_passes, run_monte_carlo
from src.backtesting.walk_forward import (
    MIN_CANDLES_FOR_OPTIMIZER,
    TradeOutcome,
    WalkForwardWindow,
    _check_sufficient_data,
    _compute_profit_factor,
    _compute_wfe,
    build_windows,
    multi_window_gate_passes,
    simulate_trade_outcome,
)
from src.config import get_settings
from src.database import AsyncSessionLocal
from src.models.candle import Candle
from src.models.optimizer_result import OptimizerResultORM

log = structlog.get_logger(__name__)

INSTRUMENT = "XAUUSD"
TIMEFRAMES = ["M15", "H1", "H4", "D1"]
BACKTEST_WINDOW_CANDLES = 500   # sliding window size — mirrors StrategyRunner
BACKTEST_STEP_CANDLES = 1       # step one candle forward per window position
MIN_TRADES_FOR_EVALUATION = 5   # skip combos generating fewer trades (insufficient sample)
N_WALK_FORWARD_WINDOWS = 3


class WalkForwardOptimizer:
    """Orchestrates LHS sampling, walk-forward backtesting, and DB param activation.

    Usage:
        optimizer = WalkForwardOptimizer()
        await optimizer.run()

    Designed to be called from an async APScheduler job. All DB access is
    async. All computation (LHS, trade simulation, Monte Carlo) is sync.

    The data guard (_check_sufficient_data) prevents param activation when
    the DB does not yet contain sufficient candles for a meaningful 3-window
    walk-forward evaluation.
    """

    def __init__(self) -> None:
        """Initialise optimizer. Strategy imports deferred to run() to avoid circulars."""
        self._settings = get_settings()

    def _sample_param_combinations(
        self,
        param_ranges: dict[str, tuple[float, float]],
        n_combos: int,
    ) -> list[dict[str, float]]:
        """Generate n_combos LHS parameter combinations for one strategy.

        Uses LatinHypercube(d=N_PARAMS) — NOT n_components= (deprecated before
        scipy 1.10; use d= in scipy 1.17.1). Values are clamped to declared
        bounds to guard against float-precision edge values (ASVS V5 / T-05-04).

        Args:
            param_ranges: Strategy PARAM_RANGES dict mapping name → (lo, hi).
            n_combos: Number of combinations (100 per CONTEXT.md locked decision).

        Returns:
            List of n_combos dicts, each mapping param_name → clamped float value.
        """
        param_names = list(param_ranges.keys())
        d = len(param_names)
        l_bounds = [param_ranges[k][0] for k in param_names]
        u_bounds = [param_ranges[k][1] for k in param_names]

        sampler = LatinHypercube(d=d)
        unit_samples = sampler.random(n=n_combos)           # shape: (n_combos, d)
        scaled_samples = scale(unit_samples, l_bounds, u_bounds)  # shape: (n_combos, d)

        combos = []
        for i in range(n_combos):
            combo: dict[str, float] = {}
            for j, name in enumerate(param_names):
                lo, hi = l_bounds[j], u_bounds[j]
                raw = float(scaled_samples[i, j])
                combo[name] = max(lo, min(hi, raw))  # clamp — T-05-04 mitigation
            combos.append(combo)
        return combos

    async def _fetch_all_candles(self) -> dict[str, list]:
        """Fetch ALL complete candles for XAUUSD across all timeframes from DB.

        Fetches the maximum available history (no LIMIT) — ordered oldest-first
        per the CLAUDE.md oldest→newest invariant. The optimizer needs the full
        history to build 3-window walk-forward splits.

        Returns:
            Dict of timeframe → list[Candle ORM], oldest-first.
        """
        candles: dict[str, list] = {}
        async with AsyncSessionLocal() as session:
            for tf in TIMEFRAMES:
                stmt = (
                    select(Candle)
                    .where(
                        Candle.instrument == INSTRUMENT,
                        Candle.timeframe == tf,
                        Candle.complete.is_(True),
                    )
                    .order_by(Candle.timestamp.asc())  # oldest-first
                )
                result = await session.execute(stmt)
                candles[tf] = list(result.scalars().all())
        log.info(
            "optimizer.candles_fetched",
            counts={tf: len(c) for tf, c in candles.items()},
        )
        return candles

    def _run_strategy_backtest(
        self,
        strategy_instance: Any,
        candle_slice: dict[str, list],
    ) -> list[float]:
        """Run strategy on a sliding 500-candle window to produce trade P&L list.

        Slides a BACKTEST_WINDOW_CANDLES window across candle_slice, calling
        strategy.generate_signals() at each position. For each signal, simulates
        the outcome using the subsequent candles.

        IMPORTANT: strategy.generate_signals() is async. This method is called
        inside a synchronous loop that runs inside an async context (run() is async).
        Use asyncio.get_event_loop().run_until_complete() is WRONG here — the loop
        is already running. Instead, this method returns a coroutine that run()
        must await. Actually: re-design as async and call with await. See run().

        This docstring is a design note. The actual implementation below is async.

        Args:
            strategy_instance: Instantiated strategy with params set.
            candle_slice: dict[str, list[Candle]] for the IS or OOS time window,
                          all timeframes, oldest-first.

        Returns:
            List of float P&L values (one per completed trade in the window).
        """
        # Implementation note: this is made async (see _run_strategy_backtest_async below)
        raise NotImplementedError("Use _run_strategy_backtest_async instead")

    async def _run_strategy_backtest_async(
        self,
        strategy_instance: Any,
        candle_slice: dict[str, list],
    ) -> list[float]:
        """Async version: slide 500-candle window across candle_slice, collect P&L.

        Slides BACKTEST_WINDOW_CANDLES window across the primary timeframe (H1)
        in BACKTEST_STEP_CANDLES increments. For each window position, calls
        strategy.generate_signals() with all 4 timeframes aligned to the same
        date range. For each returned signal, simulates outcome using the 50
        subsequent candles from that timeframe.

        Key contracts preserved:
        - Candle dict passed to strategy is oldest-first (CLAUDE.md invariant).
        - float(candle.close) cast applied before numpy ops.
        - No DB access in this method — candles pre-fetched.

        Args:
            strategy_instance: Instantiated strategy object with params.
            candle_slice: dict[str, list[Candle]] — all timeframes, oldest-first.

        Returns:
            List of float P&L values from simulated trade outcomes.
        """
        pnl_list: list[float] = []
        h1_candles = candle_slice.get("H1", [])
        n = len(h1_candles)

        if n < BACKTEST_WINDOW_CANDLES + 50:
            return pnl_list  # insufficient data for even one window + outcome check

        for start in range(0, n - BACKTEST_WINDOW_CANDLES - 50, BACKTEST_WINDOW_CANDLES):
            end = start + BACKTEST_WINDOW_CANDLES
            window_end_ts = h1_candles[end - 1].timestamp

            # Build aligned multi-TF window for this position
            window_candles: dict[str, list] = {}
            for tf, all_tf_candles in candle_slice.items():
                # Take candles up to window_end_ts (inclusive)
                tf_window = [c for c in all_tf_candles if c.timestamp <= window_end_ts]
                # Limit to last BACKTEST_WINDOW_CANDLES to mirror StrategyRunner behaviour
                window_candles[tf] = tf_window[-BACKTEST_WINDOW_CANDLES:]

            try:
                signals = await strategy_instance.generate_signals(window_candles)
            except Exception as exc:
                log.debug(
                    "optimizer.backtest_signal_error",
                    strategy=strategy_instance.__class__.__name__,
                    error=str(exc),
                )
                continue

            # Simulate each signal's outcome using the 50 candles AFTER window_end
            outcome_candles = h1_candles[end:end + 50]  # H1 for outcome check
            for signal in signals:
                try:
                    _, pnl = simulate_trade_outcome(
                        direction=signal.direction.value if hasattr(signal.direction, 'value') else signal.direction,
                        entry=float(signal.entry),
                        sl=float(signal.stop_loss),
                        tp1=float(signal.take_profit_1),
                        tp2=float(signal.take_profit_2) if signal.take_profit_2 else None,
                        subsequent_candles=outcome_candles,
                    )
                    pnl_list.append(pnl)
                except Exception as exc:
                    log.debug("optimizer.outcome_sim_error", error=str(exc))

        return pnl_list

    async def _evaluate_all_combos(
        self,
        strategy_class: Any,
        windows: list[WalkForwardWindow],
        candles_by_tf: dict[str, list],
    ) -> dict[str, Any] | None:
        """Evaluate all 100 LHS combos for one strategy across all 3 WF windows.

        For each combo:
        1. Run backtest on IS period → IS profit factor.
        2. Run backtest on OOS period → OOS profit factor.
        3. Compute WFE = oos_pf / is_pf.
        4. Collect OOS profit factors across all 3 windows.

        Returns the best combo (highest WFE among those passing WFE >= 0.50
        AND multi-window gate), or None if no combo passes all gates.

        Args:
            strategy_class: The strategy class (uninstantiated).
            windows: 3 WalkForwardWindow objects, oldest-first.
            candles_by_tf: Full candle history, all TFs, oldest-first.

        Returns:
            Dict with keys: combo, best_wfe, is_score, oos_score,
            oos_pf_per_window, trade_count, win_rate, max_dd, latest_window.
            Or None if no combo passes both gates.
        """
        combos = self._sample_param_combinations(
            param_ranges=strategy_class.PARAM_RANGES,
            n_combos=self._settings.lhs_combos,
        )

        best: dict[str, Any] | None = None
        best_wfe: float = -1.0

        for combo in combos:
            oos_pfs: list[float] = []
            is_pf_last: float = 0.0
            oos_pf_last: float = 0.0
            total_trades: int = 0
            all_oos_pnl: list[float] = []

            for window in windows:
                # Slice candles to IS period
                is_slice: dict[str, list] = {
                    tf: [c for c in cs if window.train_start <= c.timestamp < window.train_end]
                    for tf, cs in candles_by_tf.items()
                }
                # Slice candles to OOS period
                oos_slice: dict[str, list] = {
                    tf: [c for c in cs if window.oos_start <= c.timestamp < window.oos_end]
                    for tf, cs in candles_by_tf.items()
                }

                strategy = strategy_class(params=combo)

                is_pnl = await self._run_strategy_backtest_async(strategy, is_slice)
                oos_pnl = await self._run_strategy_backtest_async(strategy, oos_slice)

                if len(is_pnl) < MIN_TRADES_FOR_EVALUATION:
                    oos_pfs.append(0.0)
                    continue

                is_pf = _compute_profit_factor(np.array(is_pnl, dtype=float))
                oos_pf = _compute_profit_factor(np.array(oos_pnl, dtype=float))
                oos_pfs.append(oos_pf)

                # Track last window metrics for the final DB write
                is_pf_last = is_pf
                oos_pf_last = oos_pf
                total_trades += len(oos_pnl)
                all_oos_pnl.extend(oos_pnl)

            wfe = _compute_wfe(is_pf_last, oos_pf_last)

            if wfe < self._settings.wfe_minimum:
                continue  # WFE gate fails

            if not multi_window_gate_passes(oos_pfs):
                continue  # multi-window gate fails

            if wfe > best_wfe:
                oos_arr = np.array(all_oos_pnl, dtype=float)
                winners = oos_arr[oos_arr > 0]
                losers = oos_arr[oos_arr < 0]
                win_rate = float(len(winners) / len(oos_arr)) if len(oos_arr) > 0 else 0.0
                equity = np.cumsum(oos_arr) if len(oos_arr) > 0 else np.array([0.0])
                running_max = np.maximum.accumulate(equity)
                max_dd = float((running_max - equity).max())

                best_wfe = wfe
                best = {
                    "combo": combo,
                    "best_wfe": wfe,
                    "is_score": is_pf_last,
                    "oos_score": oos_pf_last,
                    "oos_pf_per_window": oos_pfs,
                    "trade_count": total_trades,
                    "win_rate": win_rate,
                    "max_dd": max_dd,
                    "latest_window": windows[-1],
                    "all_oos_pnl": all_oos_pnl,
                }

        return best

    async def _activate_best_params(
        self,
        strategy_name: str,
        result: dict[str, Any],
    ) -> None:
        """Write the best combo to optimizer_results with is_active=True.

        Single transaction: deactivate existing active rows for this strategy,
        then insert new is_active=True row. Follows src/database.py AsyncSessionLocal
        context manager pattern and src/pipeline/runner.py transaction pattern.

        Args:
            strategy_name: Strategy STRATEGY_NAME string.
            result: Dict from _evaluate_all_combos() containing combo and metrics.
        """
        window: WalkForwardWindow = result["latest_window"]

        async with AsyncSessionLocal() as session:
            # Deactivate previous active rows for this strategy
            await session.execute(
                update(OptimizerResultORM)
                .where(
                    OptimizerResultORM.strategy == strategy_name,
                    OptimizerResultORM.is_active.is_(True),
                )
                .values(is_active=False)
            )

            new_result = OptimizerResultORM(
                strategy=strategy_name,
                params=result["combo"],
                train_start=window.train_start,
                train_end=window.train_end,
                test_start=window.oos_start,
                test_end=window.oos_end,
                in_sample_score=result["is_score"],
                oos_score=result["oos_score"],
                wfe=result["best_wfe"],
                profit_factor=result["oos_score"],  # OOS PF stored as profit_factor
                max_drawdown=result["max_dd"],
                win_rate=result["win_rate"],
                trade_count=result["trade_count"],
                is_active=True,
            )
            session.add(new_result)
            await session.commit()

        log.info(
            "optimizer.params_activated",
            strategy=strategy_name,
            wfe=result["best_wfe"],
            trade_count=result["trade_count"],
        )

    async def run(self) -> None:
        """Run walk-forward optimizer for all 4 strategies.

        Sequence:
        1. Fetch all candles from DB (one query per timeframe).
        2. Check data sufficiency — skip and log if insufficient.
        3. Build 3 rolling walk-forward windows.
        4. For each strategy: evaluate 100 LHS combos, find best passing WFE + multi-window gate.
        5. For each strategy with a passing combo: run Monte Carlo, activate if passes.
        6. Log structured events throughout.

        Strategies that fail all gates retain their previous active params (no deactivation).
        """
        from src.strategies.liquidity_sweep import LiquiditySweepStrategy
        from src.strategies.trend_continuation import TrendContinuationStrategy
        from src.strategies.breakout_expansion import BreakoutExpansionStrategy
        from src.strategies.ema_momentum import EmaMomentumStrategy

        strategy_classes = [
            LiquiditySweepStrategy,
            TrendContinuationStrategy,
            BreakoutExpansionStrategy,
            EmaMomentumStrategy,
        ]

        log.info("optimizer.run.start")

        candles_by_tf = await self._fetch_all_candles()

        if not _check_sufficient_data(candles_by_tf):
            log.warning(
                "optimizer.skipped.insufficient_data",
                counts={tf: len(c) for tf, c in candles_by_tf.items()},
                minimums=MIN_CANDLES_FOR_OPTIMIZER,
            )
            return

        most_recent_ts = max(
            candles_by_tf[tf][-1].timestamp
            for tf in TIMEFRAMES
            if candles_by_tf.get(tf)
        )

        windows = build_windows(
            most_recent_ts=most_recent_ts,
            train_months=self._settings.wf_train_months,
            test_months=self._settings.wf_test_months,
            n_windows=N_WALK_FORWARD_WINDOWS,
        )

        for strategy_class in strategy_classes:
            strategy_name = strategy_class.STRATEGY_NAME
            log.info("optimizer.strategy.start", strategy=strategy_name)

            best = await self._evaluate_all_combos(strategy_class, windows, candles_by_tf)

            if best is None:
                log.warning(
                    "optimizer.strategy.no_passing_combo",
                    strategy=strategy_name,
                )
                # Retain previous active params — no deactivation
                continue

            # Monte Carlo validation on OOS P&L
            oos_pnl = best.get("all_oos_pnl", [])
            if len(oos_pnl) < MIN_TRADES_FOR_EVALUATION:
                log.warning(
                    "optimizer.strategy.mc_skipped_too_few_trades",
                    strategy=strategy_name,
                    trade_count=len(oos_pnl),
                )
                continue

            mc_results = run_monte_carlo(
                daily_pnl=np.array(oos_pnl, dtype=float),
                n_simulations=1000,
            )

            if not monte_carlo_passes(mc_results):
                log.warning(
                    "optimizer.strategy.mc_gate_failed",
                    strategy=strategy_name,
                    p95_dd=mc_results["p95_drawdown"],
                    p5_pf=mc_results["p5_profit_factor"],
                    hist_dd=mc_results["historical_max_drawdown"],
                )
                continue  # Retain previous params

            await self._activate_best_params(strategy_name, best)

        log.info("optimizer.run.complete")
```

Create tests/test_backtesting/test_optimizer.py with unit tests that mock DB access:

- test_lhs_produces_100_combos: instantiate WalkForwardOptimizer, call _sample_param_combinations with a 3-param dict, assert len == 100
- test_lhs_values_in_range: assert all values in all combos are within [lo, hi] bounds for each param
- test_lhs_keys_match_param_ranges: assert combo dict keys == param_ranges.keys()
- test_lhs_values_are_floats: assert all values are Python float (not np.float64 — use isinstance(v, float))
- test_data_guard_skips_when_insufficient: mock _fetch_all_candles to return sparse candles (< minimums), mock _check_sufficient_data to return False, call await optimizer.run() — assert no OptimizerResultORM was created (mock session.add and assert not called)
- test_retains_previous_on_failure: when _evaluate_all_combos returns None for a strategy, assert that update().values(is_active=False) is NOT called for that strategy (use MagicMock on session.execute)

Use pytest-asyncio (already configured in pyproject.toml with asyncio_mode = "auto") for async tests. Mock DB with AsyncMock from unittest.mock.
  </action>

  <verify>
    <automated>cd /Users/cube/Documents/00-code/0rum && python -m pytest tests/test_backtesting/test_optimizer.py -x -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_backtesting/test_optimizer.py -x -q` exits 0 with all tests passing
    - `grep -n "class WalkForwardOptimizer" src/backtesting/optimizer.py` confirms the class
    - `grep -n "LatinHypercube(d=d)" src/backtesting/optimizer.py` confirms correct scipy API (NOT n_components=)
    - `grep -n "max(lo, min(hi, raw))" src/backtesting/optimizer.py` confirms clamping (T-05-04 mitigation)
    - `grep -n "_check_sufficient_data" src/backtesting/optimizer.py` confirms data guard call inside run()
    - `grep -n "wfe_minimum" src/backtesting/optimizer.py` confirms config-driven WFE gate
    - `grep -n "is_active=False" src/backtesting/optimizer.py` confirms deactivation of old rows
    - `grep -n "retain" src/backtesting/optimizer.py` OR comment confirming skip without deactivation on gate failure
    - `python -c "from src.backtesting.optimizer import WalkForwardOptimizer; print('ok')"` prints "ok"
    - `pytest -x -q` exits 0 (full 143+ test suite green)
  </acceptance_criteria>

  <done>WalkForwardOptimizer implemented with LHS sampling (100 combos), data guard, WFE gate, multi-window gate, Monte Carlo validation, async DB write; all unit tests pass; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LHS sampled floats → strategy params | Float values after scipy scale() may slightly exceed declared bounds |
| Candle ORM .close/.high/.low → numpy | Decimal type requires explicit float() cast |
| optimizer_results DB write | Writes new is_active=True row after deactivating old rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | _sample_param_combinations() | mitigate | Clamp each value: `max(lo, min(hi, raw))` before inserting into combo dict. Verified in acceptance criteria grep. |
| T-05-05 | Tampering | _activate_best_params() DB write | accept | Internal async job with no external input path; strategy_name comes from STRATEGY_NAME class constant |
| T-05-06 | Denial of Service | _evaluate_all_combos 100×3 strategy calls | accept | max_instances=1 on APScheduler job prevents concurrent runs; 100 combos × 3 windows is bounded computation |
</threat_model>

<verification>
After task completes:

```bash
cd /Users/cube/Documents/00-code/0rum
pytest tests/test_backtesting/test_optimizer.py -x -q
pytest tests/test_backtesting/ -x -q
pytest -x -q
python -c "from src.backtesting.optimizer import WalkForwardOptimizer; print('ok')"
```
</verification>

<success_criteria>
- tests/test_backtesting/test_optimizer.py: all 6 tests pass
- WalkForwardOptimizer importable with no errors
- LHS clamping verifiable via grep
- Data guard present and tested
- Retain-previous behavior verified (no deactivation on gate failure)
- Full 143+ test suite green with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-backtesting-validation/05-02-SUMMARY.md` covering:
- Files created
- Class and method signatures confirmed
- LHS API version in use (d= not n_components=)
- Test count and result
- Any deviations from plan
</output>
