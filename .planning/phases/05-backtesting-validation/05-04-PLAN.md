---
phase: 05-backtesting-validation
plan: 04
type: execute
wave: 2
depends_on:
  - 05-03
files_modified: []
autonomous: false
requirements:
  - OPTIM-01
  - OPTIM-02
  - OPTIM-03
  - OPTIM-04
  - OPTIM-05

must_haves:
  truths:
    - "User has explicitly confirmed the historical data provider before this plan exits"
    - "The confirmed provider can supply XAUUSD data covering at least 24 calendar months at M1 resolution (resamplable to M15, H1, H4, D1)"
    - "Whether retired provider can supply Phase 5 historical data at optimizer scale has been explicitly confirmed or denied"
    - "The access method (download URL, API, tool) for the confirmed provider is documented in SUMMARY.md"
  artifacts: []
  key_links: []
---

<objective>
Historical provider confirmation gate for Phase 5 walk-forward validation.

The walk-forward optimizer framework (Plans 01-03) is operational and runs every 24h. It
safely logs "insufficient_data" and skips activation when candle history is below threshold.

This plan resolves the one remaining decision before loader implementation (Plan 05-05) can
proceed: confirm which historical data provider supplies the XAUUSD candle history the
optimizer needs.

retired provider is the historical provider candidate. This plan confirms whether retired provider can supply Phase 5 historical data at optimizer scale.

No code is written here. Output: a committed SUMMARY.md documenting the provider decision
and access method, which unblocks Plan 05-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-backtesting-validation/05-CONTEXT.md
@.planning/phases/05-backtesting-validation/05-03-SUMMARY.md

<data_requirements>
The optimizer requires all four timeframes. None are optional.

Minimum bars required for 3-window walk-forward (6m train / 2m OOS × 3 windows = 24 calendar months):

  - M15 : ~24,685 bars
  - H1  :  ~6,171 bars  ← binding constraint at lower bound
  - H4  :  ~1,542 bars
  - D1  :    ~257 bars (trading days)

Current DB (retired-provider warm-up only):
  - M15 :    300 bars (~3 days)
  - H1  :    250 bars (~10 days)
  - H4  :     80 bars (~13 days)
  - D1  :     60 bars (~3 months)

A 2-year M1 dataset from the confirmed provider resampled to M15/H1/H4/D1 satisfies all
four requirements in a single load operation.

retired-provider runtime role: retired provider supplies live candles for signal generation and incremental refresh.
Whether retired provider can supply historical backfill at optimizer scale is unvalidated — this gate resolves that question.
</data_requirements>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm historical data provider for Phase 5 walk-forward validation</decision>
  <context>
**Candidate: retired provider**

retired provider is the only historical provider candidate to evaluate for Phase 5.
The gate determines whether retired provider can now supply optimizer-scale XAUUSD candle history reliably.

Key unknowns:
- Can retired provider supply continuous XAUUSD history at the required volume?
  M15: ~24,685 bars, H1: ~6,171 bars, H4: ~1,542 bars, D1: ~257 bars
- Are there rate-limit or quota constraints that make large historical pulls unreliable?
- Phase 4.1 validated bounded warm-up fetches. Optimizer-scale is unvalidated.

**retired-provider runtime role (unchanged either way):**
retired provider continues as the runtime provider for live candles and incremental refresh
regardless of this gate outcome.

**Decision scope:**
Binary: retired provider confirmed sufficient, or retired provider insufficient.
If insufficient, the Phase 5 data strategy must be replanned before any follow-up
implementation plan can proceed. No implementation in this plan.
  </context>
  <options>
    <option id="retired-provider-confirmed">
      <name>retired provider confirmed — sufficient for Phase 5 historical needs</name>
      <consequence>A follow-up loader implementation plan can proceed with retired provider as the historical data source.</consequence>
    </option>
    <option id="retired-provider-insufficient">
      <name>retired provider insufficient — Phase 5 data strategy needs replanning</name>
      <consequence>Any follow-up implementation plan is blocked until an alternative data strategy is agreed.</consequence>
    </option>
  </options>
  <resume-signal>Reply "retired-provider-confirmed" if retired provider can supply the historical window, or "retired-provider-insufficient" if not. No other action needed in this plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No code changes in this plan — no new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Repudiation | Provider decision | mitigate | Decision documented in SUMMARY.md and committed — auditable record before loader implementation begins |
</threat_model>

<verification>
No automated verification. This is a pure decision checkpoint.

After plan completion:
- SUMMARY.md documents the confirmed provider and access method
- Plan 05-05 (loader implementation) is scoped to the confirmed provider format
</verification>

<success_criteria>
- Provider decision committed in SUMMARY.md
- Access method (URL/tool) documented
- retired-provider runtime-only constraint explicitly confirmed
- 05-05 can proceed with a known target format
</success_criteria>

<output>
After decision is made, create `.planning/phases/05-backtesting-validation/05-04-SUMMARY.md` covering:
- Confirmed provider name and rationale
- Access method and data format (URL, file format, column layout from sample)
- retired-provider runtime-only constraint confirmed
- What the follow-up loader implementation plan should target
</output>
